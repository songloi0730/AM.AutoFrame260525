<UserControl x:Class="AM.Modules.Parameter.ParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource Screen.BackgroundBrush}">

    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedParams" Source="{Binding Parameters}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Group"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Recipe / Tham số" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"
                   Foreground="{DynamicResource Text.HeadingBrush}"/>

        <!-- Thanh chọn recipe + lệnh -->
        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Recipe" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource Text.SecondaryBrush}"/>
            <ComboBox Width="200" Height="30" VerticalContentAlignment="Center"
                      ItemsSource="{Binding RecipeNames}"
                      SelectedItem="{Binding SelectedRecipeName, Mode=TwoWay}"/>
            <Button Content="Nạp" Width="70" Height="30" Margin="8,0,0,0" Command="{Binding LoadCommand}"/>
            <Button Content="Khôi phục" Width="90" Height="30" Margin="8,0,0,0" Command="{Binding ReloadCommand}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Kiểm tra" Width="90" Height="30" Command="{Binding ValidateCommand}"/>
                <Button Content="Lưu" Width="90" Height="30" Margin="8,0,0,0" Command="{Binding SaveCommand}"
                        Background="{DynamicResource Button.PrimaryBrush}"
                        Foreground="{DynamicResource Text.PrimaryBrush}" BorderThickness="0"/>
            </StackPanel>
        </DockPanel>

        <!-- Form tham số (group theo [ParamView].Group) -->
        <Border Grid.Row="2" CornerRadius="6" Padding="12"
                Background="{DynamicResource Panel.BackgroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Source={StaticResource GroupedParams}}">
                    <ItemsControl.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"
                                               Margin="0,10,0,6"
                                               Foreground="{DynamicResource Text.LiveValueBrush}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ItemsControl.GroupStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Label}" VerticalAlignment="Center"
                                           Foreground="{DynamicResource Text.PrimaryBrush}"/>
                                <TextBox Grid.Column="1" Height="28" VerticalContentAlignment="Center" Padding="6,0"
                                         Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Column="2" Text="{Binding RangeHint}" VerticalAlignment="Center"
                                           Margin="10,0,0,0" FontSize="11"
                                           Foreground="{DynamicResource Text.SecondaryBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Lỗi validate -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding ValidationErrors}" Margin="0,8,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="{DynamicResource Status.AlarmBrush}" FontSize="12"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Status -->
        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Margin="0,8,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource Text.SecondaryBrush}"/>
    </Grid>
</UserControl>

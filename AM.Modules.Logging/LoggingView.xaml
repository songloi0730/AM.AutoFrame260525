<UserControl x:Class="AM.Modules.Logging.LoggingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:AM.UI.Localization;assembly=AM.UI.Localization"
             mc:Ignorable="d"
             Background="{DynamicResource Screen.BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding [Nav.Logging], Source={x:Static loc:Loc.Strings}}"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,12"
                   Foreground="{DynamicResource Text.HeadingBrush}"/>

        <!-- Toolbar -->
        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="{Binding [Col.Level], Source={x:Static loc:Loc.Strings}}" VerticalAlignment="Center"
                       Margin="0,0,6,0" Foreground="{DynamicResource Text.SecondaryBrush}"/>
            <ComboBox Width="90" Height="30" VerticalContentAlignment="Center"
                      ItemsSource="{Binding LevelOptions}" SelectedItem="{Binding MinLevel, Mode=TwoWay}"/>
            <TextBox Width="280" Height="30" Margin="10,0,0,0" VerticalContentAlignment="Center" Padding="6,0"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding [Prod.Refresh], Source={x:Static loc:Loc.Strings}}" Width="90" Height="30"
                        Command="{Binding RefreshCommand}"/>
                <Button Content="{Binding [Log.OpenFolder], Source={x:Static loc:Loc.Strings}}" Width="120" Height="30"
                        Margin="8,0,0,0" Command="{Binding OpenFolderCommand}"/>
            </StackPanel>
        </DockPanel>

        <!-- Log lines -->
        <Border Grid.Row="2" CornerRadius="6" Padding="8"
                Background="{DynamicResource Panel.BackgroundBrush}">
            <ListBox ItemsSource="{Binding Lines}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource Text.PrimaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="DBG">
                                            <Setter Property="Foreground" Value="{DynamicResource Text.SecondaryBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="WRN">
                                            <Setter Property="Foreground" Value="{DynamicResource Status.WarningBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="ERR">
                                            <Setter Property="Foreground" Value="{DynamicResource Status.AlarmBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="FTL">
                                            <Setter Property="Foreground" Value="{DynamicResource Status.AlarmBrush}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,8,0,0"
                   Foreground="{DynamicResource Text.SecondaryBrush}"/>
    </Grid>
</UserControl>

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AM.Core.Sequencing</RootNamespace>
    <AssemblyName>AM.Core.Sequencing</AssemblyName>
  </PropertyGroup>

  <!-- Standalone có chủ đích (ADR 0011 §2): engine không reference DryIoc/hardware/UI.
       HAL đi qua contract IIoService/IMotionService trong chính project này;
       adapter nối HAL thật nằm ở AM.Infrastructure (Prompt D). -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>

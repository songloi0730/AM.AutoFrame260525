<UserControl x:Class="AM.Modules.Settings.HardwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:AM.UI.Localization;assembly=AM.UI.Localization"
             mc:Ignorable="d">

    <StackPanel MaxWidth="900" HorizontalAlignment="Left">
        <TextBlock Text="{Binding [Hw.Title], Source={x:Static loc:Loc.Strings}}"
                   FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4"
                   Foreground="{DynamicResource Text.HeadingBrush}"/>
        <TextBlock Text="{Binding [Hw.Hint], Source={x:Static loc:Loc.Strings}}"
                   FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"
                   Foreground="{DynamicResource Text.SecondaryBrush}"/>

        <ItemsControl ItemsSource="{Binding Devices}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="6" Padding="12,8" Margin="0,3"
                            Background="{DynamicResource Panel.BackgroundAltBrush}"
                            BorderBrush="{DynamicResource Border.DefaultBrush}" BorderThickness="1">
                        <DockPanel>
                            <!-- Kết nối lại từng thiết bị (Engineer+) -->
                            <Button DockPanel.Dock="Right" Height="36" Padding="14,0" Cursor="Hand"
                                    Command="{Binding DataContext.ReconnectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding DataContext.CanReconnect, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    Content="{Binding [Diag.Reconnect], Source={x:Static loc:Loc.Strings}}"/>
                            <!-- Trạng thái ● / ✕ -->
                            <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,14,0"
                                       FontSize="13" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="✕"/>
                                        <Setter Property="Foreground" Value="{StaticResource Status.AlarmBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Connected}" Value="True">
                                                <Setter Property="Text" Value="●"/>
                                                <Setter Property="Foreground" Value="{StaticResource Status.NormalBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                <Setter Property="Text" Value="…"/>
                                                <Setter Property="Foreground" Value="{DynamicResource Text.SecondaryBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource Text.PrimaryBrush}">
                                    <Run Text="{Binding Name, Mode=OneWay}"/>
                                    <Run Text=" · " Foreground="{DynamicResource Text.SecondaryBrush}"/>
                                    <Run Text="{Binding Category, Mode=OneWay}" Foreground="{DynamicResource Text.SecondaryBrush}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Driver}" FontSize="12" FontFamily="Consolas"
                                           Foreground="{DynamicResource Text.SecondaryBrush}"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="{Binding StatusText}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource Text.SecondaryBrush}"/>
    </StackPanel>
</UserControl>
